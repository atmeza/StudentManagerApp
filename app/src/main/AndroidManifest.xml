<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.des.studentmanagerredux">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">

        </activity>
        <activity android:name=".ToDoSample" />
        <activity
            android:name=".CalendarDrawer"
            android:label="@string/title_activity_calendar_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Calendar_Page" />
        <activity android:name=".ToDoList" />
        <activity android:name="com.example.des.samplehomescreen.Login">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
            </activity>
        <activity android:name="com.example.des.samplehomescreen.Register"></activity>
    </application>

</manifest>